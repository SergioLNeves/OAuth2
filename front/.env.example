# ============================================================================
# OAuth Client Configuration
# ============================================================================
VITE_OAUTH_CLIENT_ID=teste-front

# ============================================================================
# OAuth Endpoints
# ============================================================================
# Para desenvolvimento local (sem Docker):
# VITE_OAUTH_AUTH_URL=http://localhost:8080/authorize
# VITE_OAUTH_TOKEN_URL=http://localhost:8080/token

# Para desenvolvimento com Docker (comunicação interna):
VITE_OAUTH_AUTH_URL=http://backend:8080/authorize
VITE_OAUTH_TOKEN_URL=http://backend:8080/token

# Para produção:
# VITE_OAUTH_AUTH_URL=https://auth.seudominio.com/authorize
# VITE_OAUTH_TOKEN_URL=https://auth.seudominio.com/token

# ============================================================================
# Redirect URI (sempre usa localhost/domínio externo - executa no browser)
# ============================================================================
# Desenvolvimento:
VITE_OAUTH_REDIRECT_URI=http://localhost:5173/callback

# Produção:
# VITE_OAUTH_REDIRECT_URI=https://app.seudominio.com/callback
